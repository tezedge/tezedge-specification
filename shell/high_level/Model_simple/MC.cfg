CONSTANTS
    a = a
    b = b
    ValidStates = {a, b}
    peerThreshold <- peer_threshold
    connectionThreshold <- connection_threshold
    NumNodes <- num_nodes
    NumJoins <- num_joins
    sizeBound <- size_bound

SPECIFICATION
    Spec

INVARIANT
    Inv

PROPERTIES
    Prop1
    Prop2
