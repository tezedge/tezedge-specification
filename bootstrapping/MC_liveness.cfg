CONSTANTS
    MAX_OPS            <- Max_ops
    MIN_PEERS          <- Min_peers
    MAX_PEERS          <- Max_peers
    MAX_LEVEL          <- Max_level
    THRESHOLD          <- Threshold
    VALIDATOR          <- Validator
    BAD_NODES          <- Bad_nodes
    GOOD_NODES         <- Good_nodes
    CURRENT_HEAD       <- Current_head
    SAMPLES            <- Node_samples
    BLOCKS             <- Good_node_blocks
    BAD_BOOTSTRAPPING  <- Bad_bootstrapping
    GOOD_BOOTSTRAPPING <- Good_bootstrapping

SPECIFICATION
    Spec

PROPERTIES
    Liveness
